// js funtion with parameters to calculate the area of a circle
let num1=3.142
let radius=20 * 20
function area(){
    area=num1 * radius
    console.log(`The area of ${num1} and ${radius} and is ${area}`)
}
area()
// js functionto calculate the BMI
let weight=100
let height=50 * 50
function BMI(){
    BMI=weight/height
    console.log(`The BMI of ${weight} and ${height} is ${BMI}`)
}
BMI()